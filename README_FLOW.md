# Creator Flow AI — Image to Video Flow

Development notes for the image-first workflow.

Flow: generate image prompts → select/edit prompt → upload Google Flow result → select main image → generate video prompts.

This file documents the workflow while the Streamlit implementation is being updated.